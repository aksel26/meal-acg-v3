# 근태 모듈 — 기능/구조 요약 (마이그레이션용)

---

## 1. 데이터 모델

### 메인 테이블 (tblS_DayOff)

| 원본 컬럼 | 제안 컬럼명 | 타입 | 설명 |
|---|---|---|---|
| S_IDX | dayoff_id | INT (PK, Auto) | 근태 고유번호 |
| User_IDX | author_id | INT | 작성자 사번 |
| User_Name | author_name | NVARCHAR | 작성자명 |
| Last_Editor | last_editor_name | NVARCHAR | 최종 수정자명 |
| S_Date | leave_date | DATE | 근태 일자 |
| S_Hour | late_hour | VARCHAR | 지각 시 (09, 10, 11) |
| S_Minute | late_minute | VARCHAR | 지각 분 (5분 단위) |
| S_To | target_name | NVARCHAR | 대상자명 |
| S_CC | cc_names | NVARCHAR | 참조자 목록 (쉼표 구분) |
| Off_IDX | leave_type_id | INT | 근태 유형 코드 (1~16) |
| Contents | reason | NVARCHAR | 사유/내용 |
| Confirm_User | approver_name | NVARCHAR | 승인자명 (NULL=미승인) |
| Confirm_Date | approved_at | DATETIME | 승인 일시 |
| Reg_Date | created_at | DATETIME | 등록 일시 |
| Edit_Date | updated_at | DATETIME | 수정 일시 |
| Avail | is_active | INT | 유효 여부 (1=유효) |

---

## 2. 근태 유형 (16개)

```
[지각/조퇴]      1-지각, 2-조퇴
[반차]           3-오전반차, 4-오후반차
[연차]           5-연차
[대체휴무]       6-종일, 11-오전, 12-오후
[경조휴무]       7-경조휴무
[특별휴무]       8-종일, 13-오전, 14-오후
[훈련]           9-종일, 15-오전, 16-오후
[휴무]           10-휴무
```

- **통계 집계 대상**: 1~10만 (종일 단위만)
- **통계 미집계**: 11~16 (오전/오후 반일 단위) — 기존 버그

---

## 3. CRUD 처리

### 등록
- 시작일~종료일 범위를 받아 **각 날짜마다 개별 행 INSERT**
- 예: 3일 연차 → 3개 행 생성
- 주말/공휴일 필터링 **없음** (그대로 INSERT됨 — 기존 버그)

### 수정
- 수정 가능: 일자, 시:분, 참조자, 유형, 내용
- **대상자는 수정 불가** (UPDATE문에 미포함)

### 삭제
- **물리적 삭제** (DELETE FROM), 소프트 삭제 아님

### 승인
- `approver_name(승인자명)`과 `approved_at(승인일시)`만 기록
- 별도 승인 상태 플래그 없음 → `approver_name IS NULL` 여부로 판단
- **승인 취소 기능 없음**

---

## 4. 승인 워크플로우

### 상태 판별
- `approver_name(승인자명) IS NULL` → 미승인
- `approver_name(승인자명) IS NOT NULL` → 승인 완료

### 승인 후 잠금

| 동작 | 일반 직원 (미승인) | 일반 직원 (승인 후) | 관리자 |
|---|---|---|---|
| 수정 | O | X (alert 차단) | O |
| 삭제 | O | X (alert 차단) | O |
| 복사 | O | O | O |
| 승인 | O | - | - |

### 승인 권한
- 별도 역할 체크 **없음** — 로그인한 누구나 승인 가능 (기존 이슈)

---

## 5. 영업일 3일 규칙

> 지각 제외 모든 유형은 **영업일 기준 3일 이후**만 등록 가능

### 영업일 계산
- 오늘부터 1일씩 더하며 평일(월~금) + 비공휴일인 날만 카운트
- 3영업일 도달 시 그 날짜가 최소 등록 가능일

### 예외 (3일 규칙 면제)
| 조건 | 면제 |
|---|---|
| 관리자 (ADMIN) | O |
| 미승인 건 | O |
| 지각 (leave_type_id=1) | O |

### 적용 방식
- **등록**: 3일 미만 → 지각 외 유형 선택 UI 숨김
- **수정**: 3일 미만 날짜 변경 시 → alert + form.reset

---

## 6. 메일 발송

### 발송 조건

| 동작 | 대상자(To) | 참조자(CC) |
|---|---|---|
| 등록 | 체크 시 발송 (기본 미체크) | 체크 시 발송 (기본 체크됨) |
| 수정 | **발송 안 됨** | 체크 시 발송 |

### 메일 제목 패턴
- 등록 To: `[근태]등록 알림 - {대상자} {유형명}`
- 등록 CC: `[근태]참조 알림 - {대상자} {유형명}`
- 수정 CC: `[근태]수정 알림 - {대상자} {유형명}`

### 발송 방식
- 참조자는 쉼표로 분리 후 **각각 개별 메일** 발송
- 수신자 이메일은 이름 기반으로 DB 조회 (정직원만, `user_state(재직상태)=2`)
- 지각 시 유형명에 시:분 포함: `"지각-09시30분"`

---

## 7. 캘린더 뷰

- 월간 달력 (일~토, 6주 표시)
- 년/월 이동 내비게이션 + "통계보기" 버튼
- 날짜 클릭 → 등록 폼 이동
- 근태 항목 클릭 → 수정/상세 이동
- 마우스 오버 → 상세 레이어 (작성자, 수정자, 대상자, 구분, 참조, 승인자, 내용)

### 캘린더 표시 스타일
- 미승인: `[미승인] 홍길동 연차` (초록)
- 승인: `[연차] 홍길동` (노란색, 유형명 표시)

### 정렬
- leave_type_id(근태유형) → 시간 순

---

## 8. 공휴일 체계

### 양력 (고정)
신정, 삼일절, 근로자의날, 어린이날, 현충일, 광복절, 개천절, 한글날, 크리스마스

### 음력 (변환 필요)
- 설날: 음력 1/1 ± 1일 (3일)
- 석가탄신일: 음력 4/8
- 추석: 음력 8/15 ± 1일 (3일)

### 대체휴일 로직
- 어린이날 토/일 → 다음 월요일
- 설/추석 일/월/토 → +2일
- 석가탄신일 일/월/토 → +2일

---

## 9. 통계/엑셀

### 통계 팝업
- 해당 월 근태 기록이 있는 직원별, 유형별 건수 집계
- leave_type_id(근태유형) 1~10만 집계 (11~16 누락 — 버그)
- 건수 > 0이면 빨간색 강조

### 엑셀 다운로드
- 통계와 동일 구조를 엑셀로 출력
- 파일명: `근태현황_{년}년{월}월.xlsx`

---

## 10. 등록 폼 입력 필드

| 필드 | 형태 | 필수 | 비고 |
|---|---|---|---|
| 대상자 | 드롭다운 (정직원 목록) | O | |
| 시작일~종료일 | 텍스트 + 캘린더 팝업 | O | |
| 구분 | 라디오 버튼 (16개) | O | |
| 시:분 | 드롭다운 | 지각만 | 시: 9,10,11 / 분: 5분 단위 |
| 참조자 | 텍스트 + 직원선택 도우미 | X | 쉼표 구분 형식 |
| 내용 | textarea | X | |
| 메일발송(To) | 체크박스 | X | 기본 미체크 |
| 메일발송(CC) | 체크박스 | X | 기본 체크 |

### 복사 기능
- 수정 화면에서 "복사" → 등록 폼으로 이동하며 기존 데이터 미리 채움

---

## 11. 알려진 버그/이슈 (마이그레이션 시 개선 포인트)

| # | 이슈 | 개선 방향 |
|---|---|---|
| 1 | 통계에서 leave_type_id(근태유형) 11~16 미집계 | 전체 유형 집계 |
| 2 | 날짜 범위 INSERT 시 주말/공휴일 포함 | 영업일만 INSERT |
| 3 | 수정 시 대상자에게 메일 미발송 | 발송 옵션 추가 |
| 4 | 승인 권한 무제한 | 역할 기반 승인 권한 |
| 5 | 승인 취소 불가 | 승인 취소 기능 추가 |
| 6 | 동명이인 메일 발송 오류 | ID 기반 조회로 변경 |
| 7 | SQL 인젝션 취약 (날짜, 숫자 파라미터) | 파라미터 바인딩 |
| 8 | 물리적 삭제 | 소프트 삭제 고려 |
| 9 | 수정 폼에서 경조휴무 라디오 버튼 누락 | 전체 유형 표시 |
| 10 | leave_type_id(근태유형) 16 이름 불일치 (메일 시) | 통일 |
