# PRD — 인트라넷 요청 관리 모듈 (Internal Request Workflow)

| | |
|---|---|
| **문서 버전** | v0.1 (초안) |
| **작성일** | 2026-05-09 |
| **작성자** | hmkim@acghr.co.kr |
| **상태** | Draft — 검토/승인 대기 |
| **관련 데이터** | `team_pages_all.json` · `insights_team.md` · `pages_2026_with_comments.json` |

---

## 1. Executive Summary

현재 사내 팀 간 작업 요청·이슈 트래킹을 **Notion '해주세요' 페이지(7개 부서별 DB)** 로 운영 중. 2024년부터 867건의 요청과 2,052건의 댓글이 누적되었으나, 자유서식 기반이라 **상태 추적·SLA 관리·재발 패턴 식별이 불가능**한 상태다.

본 모듈은 **Notion을 전면 대체하는 것이 아니라**, 정형화된 요청 워크플로우만 인트라넷에 흡수한다. 자유 문서·회의록·위키는 Notion에 그대로 둔다.

**핵심 가치 제안:**
- 요청 처리 SLA를 **측정 가능**하게 (현재는 측정 자체가 안 됨)
- 같은 종류의 반복 요청을 **재사용 가능한 자산**으로 (현재는 매번 0부터)
- HR 시스템(공고·응시자·GIDX)과 **자동 연동**으로 입력 부담 감소

---

## 2. 배경 및 문제 정의

### 2.1 현재 운영 방식

```
Notion '해주세요' 페이지
├── HR 운영팀 DB         (510건, 댓글 685)
├── Assessment 1팀 DB    (151건, 댓글 388)
├── People & Culture 팀  (104건, 댓글 59)
├── 컨설팅 || PA팀        ( 36건, 댓글 360)
├── Assessment 2팀        ( 23건, 댓글 255)
├── HR TECH 팀           ( 23건, 댓글 135)
└── 면접 & 교육운영팀     ( 20건, 댓글 170)
```

### 2.2 데이터로 확인된 문제

| 문제 | 측정값 | 영향 |
|---|---|---|
| **종료 여부 추적 불가** | discussion `resolved=true` 비율 1.9% (2/105) | 미해결/완료 구분 불가, 리포팅 안 됨 |
| **분류 컨벤션 자율** | 제목 `[클라이언트]` 패턴 적용률 51% | 검색 누락, 통계 부정확 |
| **SLA 미측정** | 시스템상 응답시간 추적 없음 (역산만 가능: P50 3.9h, P90 142h) | 클라이언트 약속 위반 사전 감지 불가 |
| **재발 식별 불가** | "SK 규준상이 출력 요청"이 2026년에만 15+회 반복 | 같은 작업이 매번 신규 처리 |
| **HR 시스템과 단절** | GIDX·공고 정보를 매번 손으로 복사 | 입력 오류, 추적 단절 |
| **권한·접근 통제 부재** | 페이지 권한이 페이지 단위로만 | 클라이언트별 격리 어려움 |

### 2.3 왜 Notion으로 안 되는가

- Notion은 **자유 협업 도구**로 설계됨 → 워크플로우 강제·SLA·자동화 약함
- properties 강제는 가능하지만 **상태 머신·이벤트 트리거 부재**
- HR 시스템과 **양방향 연동 불가** (Webhook/API 제약)
- HR 데이터(응시자·검사 결과)가 외부 SaaS에 보관되는 **데이터 주권 이슈**

---

## 3. 목표 / Non-Goals

### 3.1 In Scope (본 모듈이 할 일)

- ✅ 사내 팀 간 작업 요청 생성·할당·진행·완료 워크플로우
- ✅ 요청 분류(클라이언트/유형/팀)와 상태 추적
- ✅ 댓글 기반 토론 + 처리 결과 정형화
- ✅ HR 시스템 핵심 엔티티(공고/응시자/GIDX) 연동·자동 첨부
- ✅ SLA 측정·알림·대시보드
- ✅ 과거 Notion 데이터 검색 가능한 읽기 전용 미러 (신규는 인트라넷 사용)
- ✅ 유사 요청 자동 추천 (Phase 3)

### 3.2 Out of Scope (본 모듈이 안 할 일)

- ❌ **기존 Notion 데이터(867건) 마이그레이션** — 본 모듈은 신규 요청만 받음. 기존 데이터는 분석/뼈대 설계용으로만 사용하고 Notion에 그대로 보존(history archive)
- ❌ 회의록·자유 문서·위키 기능 (Notion 유지)
- ❌ 자유서식 페이지 트리·중첩 페이지 구조
- ❌ 일반 DB(차량신청·BlogPost·Daily Log 등) 이전
- ❌ 외부 클라이언트 직접 접속(추후 별도 검토)
- ❌ 모바일 네이티브 앱 (반응형 웹으로 대응)

> **마이그레이션 정책 명시**: 기존 진행 중 요청은 Notion에서 끝내고 신규부터 인트라넷 사용. 컷오버 시점 이후 신규는 인트라넷 일원화. 과거 검색은 [§6.8 Notion 미러](#68-notion-미러--과거-검색)를 통해 가능.

### 3.3 성공 지표 (Goals)

| KPI | 현재 | 목표 (Phase 1+6개월) | 목표 달성을 위해 필요한 정책 변화 |
|---|---|---|---|
| 신규 요청 인트라넷 입력 비율 | 0% | 90%+ | 컷오버 + 챔피언 지정 |
| `[클라이언트]/유형` 분류율 | 51% | 99% | 마스터 강제 입력 (시스템) |
| 첫 응답 시간 P90 | 142h | **96h** (1차) → 48h (2차) | SLA 알림 + 인력 운영 룰 (도구만으로 불가) |
| 24h 이내 응답률 | 측정 불가 → 추정 65% | 80% (1차) → 85%+ (2차) | 우선순위 운영 룰 |
| 요청자 NPS (분기 설문) | - | 측정 시작 + 8 이상 | 별도 설문 운영 |
| 재사용된 과거 사례 매칭 클릭 | - | 신규 요청의 30%+ | 임베딩 추천 모델 (Phase 3) |

> **경고**: P90 응답시간 목표는 도구 교체만으로 달성 불가. **인력 운영 룰(우선순위·당직·기한 위반 에스컬레이션)** 변화가 동반되어야 함. 본 PRD는 그 측정·집행 기반만 제공한다.

---

## 4. 대상 사용자 (Personas)

데이터에서 도출된 실제 페르소나 기반.

### 4.1 요청자 (Requester) — "클라이언트 PM"
- **대표 인물**: 김소윤(119건), 김낙균(100건), 홍찬미(74건)
- **소속**: Assessment 운영, 컨설팅, PA팀
- **행동 패턴**: 클라이언트로부터 요청 받음 → 사내 처리팀에 전달
- **Pain**: 매번 같은 정보(공고 ID, 응시자 명단)를 손으로 복사, 진행 상황 추적 어려움
- **니즈**: 요청 빠른 작성 / 진행 상황 한 눈에 / 클라이언트에 보고할 통계

### 4.2 응답자 (Responder) — "처리 담당"
- **대표 인물**: 신효은(291건), 김정현(207건), 김현민(197건)
- **소속**: HR운영팀, HR TECH팀, 개발팀
- **행동 패턴**: 받은 요청을 시스템 작업으로 처리 → 결과 보고
- **Pain**: 우선순위 판단 어려움(다 급해 보임), 같은 작업 반복, 누락 발생
- **니즈**: 내 큐 명확 / SLA 기한 가시화 / 과거 처리 이력 빠른 검색

### 4.3 팀 리더 (Team Lead)
- **소속**: 7개 팀 각 리더
- **행동 패턴**: 팀 부하·진행 모니터링, 미처리 건 에스컬레이션
- **Pain**: 현재 대시보드 부재로 손으로 집계
- **니즈**: 팀 KPI 대시보드 / 기한 임박/초과 알림 / 사람별 부하

### 4.4 운영 관리자 (Ops Admin)
- **행동 패턴**: 마스터 데이터(클라이언트·요청유형) 관리, 권한 설정, 분기 회고 자료 생성
- **니즈**: 분류 체계 유지보수 / 리포트 export

---

## 5. 핵심 사용 시나리오 (User Stories)

### 5.1 요청 작성 (Requester)
> **As a** Assessment 운영팀 김소윤
> **I want** SK하이닉스 결과레포트 출력 요청을 1분 안에 작성
> **So that** 클라이언트 응답 시간을 단축

**Flow:**
1. 인트라넷 메인 → "요청하기" 버튼
2. 클라이언트 = SK하이닉스 (자동완성)
3. 요청 유형 = 결과레포트 출력 (드롭다운)
4. **HR 시스템에서 GIDX 검색 → 자동 첨부**
5. 본문(자유 텍스트) + 첨부 파일
6. 담당팀은 요청유형으로 자동 추천 → 확인
7. **유사 과거 요청 3건 표시** → 참고 또는 무시
8. 제출 → 즉시 알림 발송

### 5.2 요청 처리 (Responder)
> **As a** HR 운영팀 신효은
> **I want** 내가 처리해야 할 요청을 우선순위별로 보고
> **So that** SLA 위반 없이 처리

**Flow:**
1. 인트라넷 메인 → "내 큐" (개인 대시보드)
2. 정렬: 기한 임박순 / 클라이언트 중요도 / 요청 시간
3. 요청 클릭 → 상세 화면
4. 상태를 "진행"으로 변경 → 자동 알림
5. 댓글로 진행 보고
6. 작업 완료 → **"📌 처리 결과" 폼 입력 (필수)**: 결과 요약, 근본 원인, 재발 방지
7. 상태 "완료" → 요청자 알림 + 통계 반영

### 5.3 팀 모니터링 (Team Lead)
> **As a** Assessment 1팀 리더
> **I want** 팀 미처리 건과 기한 위험을 한 화면에
> **So that** 사전 에스컬레이션

**Flow:**
1. "팀 대시보드"
2. 위젯: 진행중 N건 / 기한임박 N건 / 기한초과 N건 / 사람별 부하 차트 / 클라이언트별 처리 추이
3. 기한초과 클릭 → 해당 건 즉시 확인 가능

### 5.4 과거 검색 (모든 사용자)
> **As a** any user
> **I want** Notion에 있던 과거 처리 이력을 인트라넷에서 검색
> **So that** 별도 도구 전환 없이 컨텍스트 확인

**Flow:**
1. 검색바에 "SK 규준상이"
2. 결과: (a) 인트라넷 신규 요청 (b) Notion 미러 데이터 모두 표시
3. Notion 결과 클릭 시 인트라넷 내 읽기전용 화면 또는 Notion 원본 링크

---

## 6. 기능 요구사항

우선순위: **P0** (MVP 필수) / **P1** (Phase 2) / **P2** (Phase 3)

### 6.1 요청 CRUD

| ID | 요구사항 | 우선순위 |
|---|---|---|
| F-1.1 | 요청 생성 (제목·본문·첨부) | P0 |
| F-1.2 | 마스터에서 클라이언트·요청유형·담당팀·우선순위 선택 (자유입력 불가) | P0 |
| F-1.3 | 요청자(작성자) 자동 기록 | P0 |
| F-1.4 | 첨부파일 (이미지·문서·5MB이상은 외부 스토리지 링크) | P0 |
| F-1.5 | 임시저장 / Draft | P1 |
| F-1.6 | 요청 수정·취소 (작성자만, 진행 시작 전까지) | P0 |
| F-1.7 | 요청 복제 (유사 요청 빠른 등록) | P1 |

### 6.2 워크플로우 / 상태 관리

| ID | 요구사항 | 우선순위 |
|---|---|---|
| F-2.1 | 상태값 6개 (§6.2.1 상태머신 참조) | P0 |
| F-2.2 | 상태 전이 규칙 + 권한 강제 (§6.2.1) | P0 |
| F-2.3 | 담당자 1차 자동 할당: **전문영역 매칭 + 부재 제외 + 부하 분산** 하이브리드 (§6.2.2) | P0 |
| F-2.4 | SLA 자동 산정: 요청유형별 기본값 (§6.2.3 계산룰) + 수동 오버라이드 | P0 |
| F-2.5 | 기한 임박/초과 자동 알림 (24h 전, 임박, 0h, +24h) | P0 |
| F-2.6 | "처리 결과" 구조화 필드 (요약·원인·재발방지) — 완료 시 필수 + 품질 룰 (§6.2.4) | P0 |
| F-2.7 | 우선순위 P1~P3 — **요청유형/클라이언트별 기본값 + 팀리더만 변경 가능** | P0 |
| F-2.8 | 에스컬레이션 룰 (SLA 위반 시 팀장 자동 통지, +48h 시 운영진) | P1 |

#### 6.2.1 상태 머신

**6개 상태**: `접수 / 진행 / 대기 / 완료 / 거절 / 보류`

| 전이 | 누가 | 조건 |
|---|---|---|
| (신규) → **접수** | 요청자 | 필수 필드 모두 채움 |
| 접수 → **진행** | 담당팀(자동할당된 본인) | 담당자 본인이 시작 선언 |
| 접수 → **거절** | 담당팀 | 거절 사유 필수 — 잘못된 팀, 정보 부족, 중복 등 |
| 접수 → **보류** | 담당팀 | 보류 사유 + 재개 예정일 필수 |
| 진행 → **대기** | 담당자 | "요청자 응답 대기" 등 외부 의존 — SLA 시계 일시정지 |
| 대기 → **진행** | 요청자 댓글 작성 / 담당자 수동 | 자동(댓글 시) + 수동 모두 가능 |
| 진행 → **완료** | 담당자 | **처리 결과(§6.2.4) 입력 필수** |
| 진행 → **보류** | 담당자 | 보류 사유 + 재개 예정일 필수 |
| 보류 → **진행** | 담당자 / 자동 | 재개 예정일 도달 시 자동 알림 |
| 완료 → **재오픈(진행)** | 요청자 (24h 내) / 팀리더 (이후) | 재오픈 사유 필수, 별도 토픽 |
| 거절 → ❌ | — | 종결 (재요청은 신규 생성) |

**SLA 시계**: `접수`/`진행`에서만 흐름. `대기`/`보류`/`완료`/`거절`은 정지.

#### 6.2.2 자동 할당 알고리즘

```
Phase 1 (P0): 후보 = 담당팀 멤버 ∩ 활성(휴가·부재 제외)
              점수 = (전문영역 일치 +3) + (현재 부하 역수 +2) + (라운드로빈 우선 +1)
              최고 점수 1명 자동 지정. 동점 시 라운드로빈.
Phase 2 (P1): 클라이언트별 전담자 우선, 그 외는 위 알고리즘.
Phase 3 (P2): 과거 처리 이력 기반 ML 추천.
```
- **부재 처리**: HR 시스템 연동 또는 사용자 본인 설정(휴가 시작/종료)
- **수동 재할당**: 팀리더 권한, 사유 기록(감사 로그)

#### 6.2.3 SLA 계산 룰

- **기준 시간**: KST(UTC+9), 영업일 09:00~18:00 기준
- **영업일 정의**: 평일 - 한국 법정 공휴일 - 사내 휴무일 (마스터 캘린더)
- **SLA 시계 정지**: 영업외 시간(주말·공휴일·야간), `대기`/`보류` 상태
- **요청유형별 기본 SLA** (Phase 0 합의 후 확정 — 초안):

| 유형 | 기본 SLA (영업시간) |
|---|---|
| 버그(P1) | 4h |
| 출력 / 데이터 요청 | 8h (영업일 1일) |
| 세팅 | 16h (영업일 2일) |
| 문의 | 24h (영업일 3일) |
| 기타 | 24h (영업일 3일) |

- **우선순위별 가중**: P1=×0.5, P2=×1.0, P3=×2.0
- **수동 오버라이드**: 팀리더+ 권한, 사유 필수, 감사 로그

#### 6.2.4 "처리 결과" 입력 품질 룰

`완료` 전이 시 강제 입력. **형식적 입력을 막기 위한 최소 품질 룰**:

| 필드 | 룰 |
|---|---|
| 결과 요약 | 최소 30자, 마크다운 허용 |
| 근본 원인 | 최소 20자, 또는 "해당 없음(정상 요청)" 명시 체크박스 |
| 재발 방지 | 최소 20자, 또는 "재발 방지 불요(일회성)" 명시 체크박스 |

- **샘플 검수**: 팀리더가 월 1회 무작위 5건 샘플링 → 형식적 입력 시 보완 요청 (운영 룰)
- **유예 기간(grace)**: Phase 1 출시 후 4주는 권고만, 5주차부터 강제
- **템플릿 제공**: 요청유형별 결과 템플릿 (3~5종)으로 입력 부담 완화

### 6.3 댓글 / 토론

| ID | 요구사항 | 우선순위 |
|---|---|---|
| F-3.1 | 마크다운 지원 댓글 작성·수정·삭제(작성자) | P0 |
| F-3.2 | @멘션 (사내 사용자) → 알림 | P0 |
| F-3.3 | 댓글 리액션 (👍/✅) | P1 |
| F-3.4 | 댓글 검색 (요청 내) | P1 |
| F-3.5 | 댓글 LLM 자동 요약 (스레드 → 한 줄 결론) | P2 |

### 6.4 검색 / 필터

| ID | 요구사항 | 우선순위 |
|---|---|---|
| F-4.1 | 전문 검색 (제목·본문·댓글) | P0 |
| F-4.2 | 필터: 클라이언트·유형·팀·상태·기간·담당자 | P0 |
| F-4.3 | 저장된 필터 (즐겨찾기) | P1 |
| F-4.4 | Notion 미러 데이터 통합 검색 | P0 |
| F-4.5 | 유사 요청 자동 추천 (작성 중 / 상세 화면 사이드) | P2 |

### 6.5 대시보드 / 리포트

| ID | 요구사항 | 우선순위 |
|---|---|---|
| F-5.1 | 개인 대시보드 (내가 만든 요청 / 내가 처리할 요청) | P0 |
| F-5.2 | 팀 대시보드 (진행/지연/완료/사람별 부하) | P0 |
| F-5.3 | 클라이언트별 활동 차트 | P1 |
| F-5.4 | SLA 리포트 (P50/P90, 위반 케이스) | P1 |
| F-5.5 | 분기 회고 자료 자동 export (PDF/CSV) | P1 |
| F-5.6 | 재발 패턴 알람 (같은 유형 N회 이상 반복 시) | P2 |

#### 6.5.1 위젯 정의 (대시보드 P0 위젯 7종)

| 위젯 | 계산식 | 새로고침 |
|---|---|---|
| **상태별 카운트** | 팀의 모든 요청을 6개 상태로 그룹 카운트 | 30초 캐시 |
| **사람별 부하** | 멤버별 `진행+대기` 상태 건수 / SLA 가중치 합산 | 1분 캐시 |
| **기한 임박 리스트** | `sla_due_at - now() <= 24h` AND 상태 ∈ {접수, 진행} | 1분 캐시 |
| **기한 초과 리스트** | `sla_due_at < now()` AND 상태 ∈ {접수, 진행} — 빨강 | 1분 캐시 |
| **클라이언트별 7일 추이** | 클라이언트별 일별 신규 요청 수 라인 차트 | 5분 캐시 |
| **SLA 위반 건수 (이번 달)** | 이번 달 완료된 요청 중 `closed_at > sla_due_at` 카운트 | 5분 캐시 |
| **유형별 처리 시간 분포** | 이번 달 완료 요청의 유형별 P50/P90 박스플롯 | 5분 캐시 |

### 6.6 HR 시스템 연동

| ID | 요구사항 | 우선순위 |
|---|---|---|
| F-6.1 | 공고(GIDX) 검색·자동 첨부 | P0 |
| F-6.2 | 응시자 식별자(ID·이름) 첨부 (PII 통제 룰 준수) | P0 |
| F-6.3 | 클라이언트 마스터 자동 동기화 | P0 |
| F-6.4 | HR 시스템 작업 완료 → 인트라넷 요청 자동 상태 변경 (옵션) | P1 |
| F-6.5 | 양방향 링크 (HR 화면 → 관련 요청, 요청 → HR 데이터) | P1 |

### 6.7 권한 / 보안

| ID | 요구사항 | 우선순위 |
|---|---|---|
| F-7.1 | SSO (인트라넷 기존 인증과 통합) | P0 |
| F-7.2 | 역할: requester / responder / team_lead / admin | P0 |
| F-7.3 | 클라이언트별 접근 제어 (특정 클라이언트는 지정된 팀만) | P0 |
| F-7.4 | 감사 로그 (상태 변경·삭제·권한 변경) | P0 |
| F-7.5 | PII 마스킹 (응시자 정보 노출 시 권한 검증) | P0 |

#### 6.7.1 권한 모델 — RBAC + 클라이언트 ACL 하이브리드

- **역할 (RBAC)**: `requester` / `responder` / `team_lead` / `ops_admin` / `super_admin`
- **클라이언트 ACL**: 클라이언트는 화이트리스트 기반 — 권한이 있는 팀/사용자만 해당 클라이언트 요청 조회·작성 가능
- **PII 표시 권한**: `responder` 이상이면서 해당 클라이언트 ACL을 가진 사용자만 응시자 식별정보 평문 표시 가능 (그 외에는 마스킹)
- **PII 검출 룰 (P0 단순)**: 정규식 기반 — 한국 주민번호, 전화번호, 이메일, 응시자 ID 패턴. ML 기반 검출은 P2.

| 행위 | requester | responder | team_lead | ops_admin |
|---|:---:|:---:|:---:|:---:|
| 요청 생성 | ✅ | ✅ | ✅ | ✅ |
| 본인 요청 수정 (진행 전) | ✅ | ✅ | ✅ | ✅ |
| 다른 사람 요청 보기 (같은 클라이언트 ACL) | ✅ | ✅ | ✅ | ✅ |
| 상태 전이 (진행/완료/거절) | ❌ | ✅(본인 할당) | ✅(팀) | ✅(전체) |
| 담당자 재할당 | ❌ | ❌ | ✅(팀) | ✅(전체) |
| 우선순위 P1 지정 | ❌ | ❌ | ✅ | ✅ |
| SLA 오버라이드 | ❌ | ❌ | ✅ | ✅ |
| 마스터 데이터 변경 | ❌ | ❌ | ❌ | ✅ |
| 감사 로그 조회 | ❌ | ❌ | 본인팀만 | 전체 |

### 6.8 Notion 미러 — 과거 검색

> **정책**: 기존 데이터 마이그레이션 ❌. 미러는 **읽기 전용 검색 인덱스**로만 사용. 신규 요청은 인트라넷에 직접 입력.

| ID | 요구사항 | 우선순위 |
|---|---|---|
| F-8.1 | 일일 자동 추출 cron (기존 `build_team_report.py` 운영화) | P0 |
| F-8.2 | 추출 데이터를 인트라넷 검색 인덱스에 저장(읽기 전용) | P0 |
| F-8.3 | Notion 원본 URL 보존 — 검색 결과에서 원본 클릭 시 새 탭 이동 | P0 |
| F-8.4 | ~~과거 요청 일괄 import~~ → **out-of-scope (마이그레이션 안 함)** | — |
| F-8.5 | 미러 Sync 실패 알림 (운영팀 채널) | P0 |
| F-8.6 | 미러 데이터 retention: 영구 보존(읽기) — Notion에서 삭제되면 미러도 7일 후 삭제 (감사 후) | P1 |

### 6.9 알림 (Notification) 스펙

#### 6.9.1 트리거 × 채널 매트릭스

| 트리거 이벤트 | 수신자 | 인앱 | 이메일 | 슬랙(or 카톡워크) |
|---|---|:---:|:---:|:---:|
| 신규 요청 생성 | 담당팀(자동할당된 본인) | ✅ | ✅ | ✅ |
| 신규 요청 생성 | 같은 팀(read-only) | ✅ | ❌ | ❌ |
| 상태 전이(접수→진행) | 요청자 | ✅ | ❌ | ❌ |
| 상태 전이(→대기) | 요청자 | ✅ | ✅ | ❌ |
| 상태 전이(→완료) | 요청자 | ✅ | ✅ | ❌ |
| 상태 전이(→거절) | 요청자 | ✅ | ✅ | ✅ |
| 댓글 추가 | 요청자·담당자·@멘션 대상 | ✅ | digest† | ✅(@멘션만) |
| @멘션 | 멘션 대상 | ✅ | ✅ | ✅ |
| SLA 24h 임박 | 담당자 | ✅ | ❌ | ✅ |
| SLA 위반(0h) | 담당자, 팀리더 | ✅ | ✅ | ✅ |
| SLA +24h 초과 | 팀리더, 운영진 | ✅ | ✅ | ✅ |
| 보류 재개 예정일 도달 | 담당자 | ✅ | ❌ | ✅ |
| 미러 Sync 실패 | ops 채널 | ❌ | ❌ | ✅ |

> †digest = 1시간 모아서 1회 발송

#### 6.9.2 사용자 수신 설정

- 채널별 **on/off 토글** (인앱은 항상 on, 이메일·슬랙은 사용자가 끌 수 있음)
- **Quiet hours**: 야간(20시~08시 KST) 슬랙·이메일 발송 보류 → 다음 영업시간에 발송 (단 SLA 위반은 즉시)
- **유형별 묶음**: 댓글류는 1시간 digest 가능

#### 6.9.3 중복 방지

- 같은 이벤트가 여러 트리거를 동시에 만족 시 **하나의 알림으로 통합** (예: @멘션 + 댓글)
- 채널별 dedup key: `event_type:request_id:user_id:hour` 단위

#### 6.9.4 알림 채널 결정

> 본 섹션은 §12 Open Question Q3 결정 후 확정. 현재 표는 가능한 채널 가정.

---

## 7. 데이터 모델 (스키마 초안)

```sql
-- 마스터 테이블
clients               (id, name, code, active, created_at)
request_types         (id, name, default_sla_hours, default_team_id)
teams                 (id, name, lead_user_id, active)
users                 (id, email, name, team_id, role, active)

-- 핵심 엔티티
requests (
  id              BIGINT PK,
  title           VARCHAR(255) NOT NULL,
  body            TEXT,
  client_id       FK clients,
  request_type_id FK request_types,
  team_id         FK teams,            -- 처리 담당팀
  requester_id    FK users,
  assignee_id     FK users NULL,       -- 1차 담당자
  status          ENUM('접수','진행','대기','완료','보류') DEFAULT '접수',
  priority        ENUM('P1','P2','P3') DEFAULT 'P3',
  sla_due_at      TIMESTAMP,
  created_at      TIMESTAMP,
  closed_at       TIMESTAMP NULL,
  -- 처리 결과 (완료 시 필수)
  result_summary  TEXT NULL,
  root_cause      TEXT NULL,
  prevention      TEXT NULL,
  -- 검색용
  search_vector   tsvector
);

comments (
  id          BIGINT PK,
  request_id  FK requests,
  author_id   FK users,
  body        TEXT,
  created_at  TIMESTAMP,
  edited_at   TIMESTAMP NULL,
  alive       BOOL DEFAULT TRUE
);

-- HR 시스템 연동 첨부
request_hr_links (
  id          BIGINT PK,
  request_id  FK requests,
  link_type   ENUM('gidx','candidate','client'),
  external_id VARCHAR(64),
  metadata    JSONB
);

attachments (
  id          BIGINT PK,
  request_id  FK requests,
  comment_id  FK comments NULL,
  file_name   VARCHAR(255),
  storage_url VARCHAR(512),
  size_bytes  BIGINT,
  uploaded_by FK users,
  uploaded_at TIMESTAMP
);

-- 상태 변경 이력 (감사)
audit_log (
  id          BIGINT PK,
  request_id  FK requests,
  actor_id    FK users,
  action      VARCHAR(64),    -- status_change, assign, edit_field 등
  before      JSONB,
  after       JSONB,
  occurred_at TIMESTAMP
);

-- Notion 미러 (읽기전용 검색 인덱스)
notion_mirror (
  notion_page_id   VARCHAR(64) PK,
  team_section     VARCHAR(64),
  title            TEXT,
  body_text        TEXT,
  comments_text    TEXT,
  notion_url       VARCHAR(512),
  created_at       TIMESTAMP,
  last_edited_at   TIMESTAMP,
  last_synced_at   TIMESTAMP,
  search_vector    tsvector
);
```

### 인덱스
- `requests(team_id, status, sla_due_at)` — 팀 대시보드
- `requests(assignee_id, status)` — 개인 큐
- `requests(client_id, created_at)` — 클라이언트별 추이
- `requests USING GIN(search_vector)` — 전문 검색
- `comments(request_id, created_at)` — 스레드 로딩
- `notion_mirror USING GIN(search_vector)` — 미러 검색

---

## 8. UI/UX 요구사항

### 8.1 정보 구조

```
인트라넷
├─ 홈 (개인 대시보드: 내 요청·내 큐·알림)
├─ 요청
│  ├─ 새 요청 작성  (단일 폼)
│  ├─ 전체 리스트   (필터/검색)
│  ├─ 내가 만든 것
│  ├─ 내 큐
│  └─ 상세
├─ 대시보드
│  ├─ 팀 대시보드
│  └─ 회사 전체 (admin)
├─ 검색 (인트라넷 + Notion 미러)
└─ 관리 (admin: 마스터 데이터, 권한)
```

### 8.2 핵심 화면

**[1] 새 요청 작성**
- 좌측 70%: 폼 (제목/본문/첨부 + 마스터 필드 5개)
- 우측 30%: **유사 과거 요청 추천** (제목 + 처리결과 요약 + 클릭→상세 새창)

**[2] 요청 상세**
- 상단: 메타(상태/담당/SLA) + 액션(상태변경/할당/완료처리)
- 본문: 요청 내용
- HR 첨부: 카드형 (GIDX·응시자 정보)
- 댓글: 시간순 + 마크다운 + @멘션
- 우측 사이드: 변경 이력, 유사 요청, HR 시스템 바로가기

**[3] 내 큐 (Responder)**
- 카드형 칸반 또는 리스트
- 정렬 기본: SLA 기한 임박순
- 색상: 정상/임박(노랑)/초과(빨강)

**[4] 팀 대시보드**
- 위젯: 상태별 카운트, 사람별 부하, 클라이언트별 추이, SLA 위반, 기한 임박 리스트

### 8.3 디자인 원칙

- 인트라넷 기존 디자인 시스템 따름
- 모바일 반응형 (요청 작성·확인·댓글이 모바일에서 가능해야 함, 대시보드는 데스크톱 우선)
- 키보드 단축키 (`g+r` 새 요청, `g+q` 내 큐, `/` 검색)
- 빈 상태/로딩/에러 UI 일관성

---

## 9. 비기능 요구사항

| 영역 | 요구사항 |
|---|---|
| **성능** | 리스트 페이지 P95 < 1.5s, 요청 상세 P95 < 1s, 댓글 작성 P95 < 500ms |
| **확장성** | 5년 후 누적 50,000건 상정 (현재 867건 → 연 ~500건 증가 시) |
| **가용성** | 사내 시스템 SLA 99.5% (인트라넷 기존 SLA 따름) |
| **보안** | OWASP Top 10 대응 / 감사 로그 / PII 마스킹 / SSO |
| **데이터 보존** | 요청·댓글 영구 보존 (soft delete) / 감사 로그 5년 |
| **백업** | 일 1회 풀백업 + 시간당 incremental (인트라넷 표준) |
| **관측성** | API 응답시간·에러율 메트릭, 핵심 이벤트 로깅 (요청생성, 상태변경, SLA위반) |
| **접근성** | WCAG 2.1 AA 준수 |

---

## 10. 단계별 출시 계획

| Phase | 기간 | 범위 | 출시 기준 |
|---|---|---|---|
| **Phase 0 — 미러 운영화** | 2주 | 본 추출 파이프라인을 cron으로 운영, 인트라넷 검색에 노출 | 일일 동기화 안정 동작, Notion 데이터 검색 가능 |
| **Phase 1 — MVP** | 8주 | F-1, F-2 (P0), F-3 (P0), F-4 (P0), F-5.1, F-5.2, F-7 (P0), F-8 (P0) | 요청 생성/할당/완료 워크플로우 동작 / 1개 팀 베타 |
| **Phase 2 — 본격 운영** | 8주 | P1 항목 + HR 시스템 연동(F-6.1~6.3), 분기 회고 export | 7개 팀 전체 도입, 신규 요청 90%+ 인트라넷 |
| **Phase 3 — 지능화** | 추후 | P2 항목 (유사사례 추천, LLM 요약, 재발 알람) | 별도 ROI 검토 후 진행 |

### Phase 1 마일스톤 (예시)
- W1~W2: 데이터 스키마 + 마스터 데이터 입력
- W3~W4: 요청 CRUD + 상태 워크플로우
- W5: 댓글 + 알림
- W6: 검색 + 필터
- W7: 대시보드
- W8: QA, 1개 팀 파일럿 (HR 운영팀 추천 — 볼륨 가장 큰 팀)

### 변화 관리
- Phase 1 도입 시 **Notion '해주세요' 페이지에 안내 배너** + 양쪽 운영 4주
- 4주차에 신규 요청은 인트라넷만 허용 (Notion에 작성 시 자동 안내)
- 12주차에 Notion '해주세요' 페이지를 읽기전용 처리

---

## 11. 리스크 및 의존성

| 리스크 | 영향도 | 대응 |
|---|---|---|
| 사용자 저항 (Notion에 익숙) | 중 | 1팀 파일럿 + 키보드 단축키 + 빠른 작성 폼 + 양쪽 운영기간 |
| 마스터 데이터 정합성 | 중 | Phase 0에서 클라이언트·유형 마스터 입력 작업 별도 진행 |
| **HR 시스템 API 가용성 미검증 (CRITICAL)** | 매우 고 | **Phase 0 종료 전 HR API spec 확인 필수.** 미비 시 F-6.1·F-6.2를 P0에서 P1으로 강등하고 외부 링크 첨부로 회피 — 단, GIDX 자동 첨부가 지연되면 KPI(신규 요청 인트라넷 입력률 90%) 달성 위험 |
| SLA 기본값 미합의 | 중 | 과거 데이터(P50 3.9h, P90 142h) 분석으로 유형별 기본값 제안 → 팀별 협의 |
| 분류 컨벤션 합의 지연 | 중 | "현재 데이터 기반 초안 → 팀 리더 미팅 1회로 확정" 절차 |
| 미러 검색의 PII 노출 | 고 | Notion 미러 시 PII 자동 검출·마스킹 룰 적용 |

### 의존성
- 인트라넷 기존 인증·권한 시스템
- HR 시스템 API (공고/응시자/마스터)
- 사내 객체 스토리지 (첨부파일)
- 알림 채널 (이메일·슬랙·웹훅 중 1개 이상)

---

## 12. 결정 필요 사항 (Open Questions)

| # | 질문 | 결정자 | 기한 |
|---|---|---|---|
| Q1 | 요청유형 마스터 분류 체계 (드래프트는 5개: 버그/데이터요청/세팅/출력/기타). 더 세분화? | 7개 팀 리더 | Phase 0 종료 전 |
| Q2 | 유형별 SLA 기본값 (예: 출력 4h, 데이터요청 24h, 버그 2h, 기타 48h) | 팀 리더 + 운영 | Phase 0 종료 전 |
| Q3 | 알림 채널 — 이메일 / 슬랙 / 카톡 워크 / 인트라넷 내 노티만 | 인프라팀 | Phase 1 시작 전 |
| Q4 | 첨부파일 스토리지 (S3 호환 / NAS / 인트라넷 표준 무엇인지) | 인프라팀 | Phase 1 시작 전 |
| Q5 | Notion '해주세요' 페이지 폐쇄 여부 — 읽기전용 / 완전 폐쇄 / 그대로 | 운영진 | Phase 2 종료 전 |
| Q6 | 외부 클라이언트(SK·CJ 등) 직접 접근 향후 가능성 | 운영진 | Phase 2 종료 후 별도 검토 |
| Q7 | 분류 미적용된 과거 217건 retrofit 작업 누가 | 팀 리더 | Phase 1 중 |
| Q8 | 처리 결과 입력 강제 시 저항 → grace 기간(현재 4주 제안) 합의 | 운영진 | Phase 1 출시 전 |
| Q9 | HR 시스템 API spec 확인 (GIDX·응시자·클라이언트 마스터) | HR 시스템 담당자 | Phase 0 종료 전 (CRITICAL) |
| Q10 | 클라이언트별 ACL 초기 매핑 (어느 팀이 어느 클라이언트?) | 7개 팀 리더 + 운영 | Phase 1 시작 전 |
| Q11 | 부재(휴가) 정보 출처 — HR 휴가 시스템 vs 사용자 본인 입력 | 인사팀 + 인프라 | Phase 1 시작 전 |
| Q12 | 이 PRD 자체 승인 절차·승인자·Frozen 시점 | 프로덕트 오너 | 즉시 |

---

## 13. 부록 — 데이터 분석 요약

### 13.1 활동 분포
- **연도별**: 2024년 363건 / 2025년 350건 / 2026년 154건 (5월 시점)
- **응답시간**: P50 3.9시간 / P90 142시간 (중앙값은 양호하나 long-tail 큼)
- **댓글 밀도**: Assessment 2팀 11.1건/페이지 (최대), People & Culture 0.57건 (최저)

### 13.2 핵심 인물 (전체 누적)
- **요청자 TOP 5**: 김소윤(119), 김낙균(100), 홍찬미(74), 윤지현(54), 이다빈(52)
- **응답자 TOP 5**: 신효은(291), 김정현(207), 김현민(197), 이승현(125), 김대희(108)

### 13.3 클라이언트 분포 (2026 기준)
- SK 50건, CJ 29건, LG 15건, SKCT 10건, 두산 5건 — SK 그룹이 약 35% 차지

### 13.4 데이터 출처 (재현 가능성)
- `team_pages_all.json` — 867 페이지 + 댓글 전수 (build_team_report.py 산출물)
- `pages_2026_with_comments.json` — 2026 활동 페이지 (extract_2026.py 산출물)
- `insights_team.md` / `insights_2026.md` — 본 분석 기반

### 13.5 후속 검토 필요 사항 (Deferred Items)

본 PRD v0.2 시점에 인지된 항목 중 **별도 보조 문서 또는 Phase 0 분과 검토**로 다룰 항목들. 본문에서 빠진 이유는 본 PRD의 가독성·승인 가능성 우선이며, 누락된 것은 아님.

#### 13.5.1 별도 문서로 작성 예정

| 항목 | 별도 산출물 | 담당 | 시점 |
|---|---|---|---|
| **테스트/QA 전략** | `QA_Plan.md` (단위·통합·E2E·UAT 계획, 회귀 범위) | QA | Phase 1 W1 |
| **검색 기술 스펙** | `Search_Tech_Spec.md` (한국어 토크나이저 Nori, 인덱스 동기화, 랭킹) | 백엔드 | Phase 1 W2 |
| **첨부파일 정책** | `File_Storage_Policy.md` (바이러스 스캔, 허용 확장자, 보존, 썸네일) | 인프라 + 보안 | Phase 1 W2 |
| **Notification 구현 스펙** | `Notification_Implementation.md` (큐, retry, dedup 구현) | 백엔드 | Phase 1 W3 |
| **DR / 백업 / 롤백 플랜** | `DR_Runbook.md` (RTO/RPO 목표, Phase 1 롤백 절차) | 인프라 | Phase 1 W4 |
| **컴플라이언스 검토** | `Compliance_Review.md` (개인정보보호법, ISMS, 클라이언트 약정) | 법무 + 보안 | Phase 1 W4 |
| **비용 분석** | `Cost_Analysis.md` (Notion 라이선스 vs 개발+운영 비용 비교, ROI) | PO + 재무 | Phase 0 W1 |
| **변화 관리 플랜** | `Change_Management.md` (트레이닝, 챔피언, 단계별 확장) | PO + HR | Phase 0 W2 |

#### 13.5.2 Phase 0 분과 검토 항목

| 항목 | 검토 내용 | 영향 |
|---|---|---|
| 이메일/외부 채널 인입 | 클라이언트가 메일로 보내는 요청을 어떻게 처리? mail-to-ticket 파서? 수동 등록? | F-1 보강 |
| 외부 클라이언트 응대 룰 | 처리 완료 시 클라이언트 통지 자동/수동/사람? | F-2.6, F-3 보강 |
| 데이터 분류·보존 | PII 등급, 응시자 데이터 보존 기한, 법적 보관 의무 | F-7, NFR 보강 |
| 인프라 의존성 명시 | LDAP/AD, 메트릭 수집(Prometheus), 큐(Redis), 검색엔진(ES/OpenSearch) 표준 | NFR 구체화 |
| 모바일 핵심 시나리오 | "반응형"으로만 기술 — 실제 모바일 지원 범위(작성 vs 댓글 vs 모니터링) 결정 | UI 보강 |

#### 13.5.3 Phase 2~3에서 다룰 항목

| 항목 | 시점 | 비고 |
|---|---|---|
| 유사 요청 추천 알고리즘 (B6) | Phase 3 | 임베딩 기반 PoC 별도 검토 |
| 댓글 LLM 자동 요약 (F-3.5) | Phase 3 | 비용·정확도 검토 후 |
| PII 검출 ML 고도화 (B10) | Phase 2 | P0는 정규식 룰 기반 |
| 마스터 데이터 변경 정책 (B9) | Phase 2 | 클라이언트명 변경, 유형 폐지 시 처리 룰 |
| 댓글 편집·삭제 권한 상세 (B8) | Phase 2 | P0는 작성자만 편집, 시간 제한 미적용 |
| 베타 → 전사 단계적 확장 (D6) | Phase 2 | 1팀 → 3팀 → 7팀으로 단계화 검토 |

#### 13.5.4 모순 해소 결정 사항 (PRD v0.1 → v0.2)

| 모순 | v0.1 상태 | v0.2 결정 |
|---|---|---|
| 마이그레이션 의도 (C1) | F-8.4 "선택적" + 12주차 Notion 읽기전용 | **마이그레이션 X** 명시. Notion은 history archive로 영구 보존 |
| 권한 우선순위 (C2) | F-7.5 PII P0 / F-7.3 격리 P1 | **둘 다 P0** — PII는 격리 위에서만 의미 있음 |
| 할당 vs 페르소나 (C3) | "라운드로빈" vs "전문 영역 매칭" 니즈 | **하이브리드 알고리즘** §6.2.2 명시 |
| KPI 비현실성 (C4) | P90 142h → 48h 단일 목표 | **2단계 목표 (96h → 48h)** + 정책 변화 명시 |
| 검색 통합 우선순위 (C5) | F-4.4 P1 / Phase 0 검색 노출 | **F-4.4 P0**로 상향 |

---

## 14. 변경 이력

| 버전 | 일자 | 작성자 | 변경 내용 |
|---|---|---|---|
| v0.1 | 2026-05-09 | hmkim | 초안 작성 (데이터 분석 결과 기반) |
| v0.2 | 2026-05-09 | hmkim | 셀프 리뷰 반영: 마이그레이션 정책 명시(X), 상태머신/SLA계산/할당알고리즘/처리결과 품질룰/대시보드 위젯/권한모델/알림 스펙 추가, KPI 2단계화, 부록 13.5 (Deferred Items) 추가 |
