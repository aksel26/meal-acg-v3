---
date: 2026-02-02
status: 완료
description: Excel import/export 구조 수정, 대시보드 UI 리팩토링, 쿼리 무효화 개선
category: Frontend, Backend
type: Feature, Bug Fix, Refactor
project: Meal ACG Admin
tags:
  - Next.js
  - React Query
  - ExcelJS
  - TypeScript
  - Recharts
---

# Meal ACG Admin 데이터 관리 및 대시보드 개선

> Excel import/export 열 구조 수정, 대시보드 UI 리팩토링, 데이터 동기화를 위한 쿼리 무효화 개선

## 문제 / 목표

1. **Excel 파싱 오류**: 기존 Excel 파일 구조(A열부터 데이터 시작)와 파서 설정(B열부터 시작)의 불일치로 181건의 날짜 누락 경고 발생
2. **데이터 동기화 이슈**: import/캘린더에서 데이터 변경 후 다른 페이지에서 최신 데이터가 반영되지 않음
3. **대시보드 UX 개선 필요**: 불필요한 섹션 제거 및 유용한 정보 추가 요청
4. **사용자 입력 누락 체크**: 사용자별 식대 입력 누락 현황 확인 기능 필요

## 해결 과정

### 1. Excel Import/Export 열 구조 수정

**문제 분석**: 두 개의 Excel 파일을 비교하여 열 인덱스 차이 발견
```
파일 1 (파싱 에러): [null, 2026, 1, 1, "목", ...] - A열 null
파일 2 (정상):      [2026, 1, 1, "목", ...]      - A열부터 데이터
```

**excel-parser.ts 수정**:
```typescript
// Before (잘못된 인덱스)
const COLUMNS = {
  YEAR: 1,  // B열
  MONTH: 2, // C열
  DAY: 3,   // D열 → 실제로 "요일" 값이 들어옴
};

// After (올바른 인덱스)
const COLUMNS = {
  YEAR: 0,  // A열
  MONTH: 1, // B열
  DAY: 2,   // C열
};
```

**export/member/route.ts 수정**:
- 내역 시트 데이터가 A열부터 시작하도록 변경
- 셀 병합 적용 (A2:F2 일자, G1:Q1 직접입력 등)
- 통계 시트 수식의 열 참조 업데이트

### 2. Import 완료 시 쿼리 무효화 추가

```typescript
// import 성공 시 관련 쿼리 무효화
await queryClient.invalidateQueries({ queryKey: queryKeys.mealLogs.all });
await queryClient.invalidateQueries({ queryKey: queryKeys.stats.all });
await queryClient.invalidateQueries({ queryKey: ["dashboard"] });

// 전체 작업 완료 알림
toast.success(`가져오기 작업이 완료되었습니다. (${filesToImport.length}개 파일)`);
```

### 3. 캘린더 변경 시 쿼리 무효화 추가

식대 등록/수정/삭제/일괄입력 시 `stats.monthly` 쿼리도 무효화하여 사용자 현황 페이지에서 최신 데이터 반영

### 4. 대시보드 UI 리팩토링

**제거된 섹션**:
- 인기 가게 TOP 5
- 월별 평균 초과금 추이 차트
- 빠른 작업 버튼

**추가/개선된 기능**:
- 점심조 현황 위젯 (주간 네비게이션)
- 카드 레이아웃 개선 (아이콘, 라벨 위치 조정)
- 총 인원 카드 → /users 링크 추가
- 정산 현황에서 미정산 멤버 전체 표시 (이름순 정렬)
- URL 파라미터로 연/월 상태 유지

### 5. 사용자 입력 누락 체크 API 및 UI

**새 API 엔드포인트**: `/api/stats/incomplete-users`
- 월별 업무일 대비 미입력 현황 조회
- 사용자별 입력률 계산

**사용자 페이지 개선**:
- 입력 누락 현황 표시
- UX 개선

### 6. 기타 개선사항

- 브랜딩 로고 및 헤더 UI 개선
- 캘린더 근태 옵션 개선
- 금액 입력 필드 '원' 텍스트와 커서 겹침 수정 (User App)
- 대시보드 금액 표시 소수점 개선

## 기술 개념 설명

### React Query invalidateQueries

React Query에서 캐시된 데이터를 무효화하여 다음 접근 시 새로운 데이터를 가져오도록 하는 메커니즘. 데이터 변경 후 관련 쿼리를 무효화하면 다른 컴포넌트에서도 최신 데이터를 볼 수 있음.

```typescript
// 특정 쿼리 무효화
queryClient.invalidateQueries({ queryKey: queryKeys.mealLogs.all });

// 접두사로 시작하는 모든 쿼리 무효화
queryClient.invalidateQueries({ queryKey: ["dashboard"] });
```

### ExcelJS 셀 병합

```typescript
// 셀 병합
detailSheet.mergeCells("A2:F2"); // 일자
detailSheet.mergeCells("G1:Q1"); // 직접 입력
detailSheet.mergeCells("G2:G3"); // 근태
```

### SheetJS (xlsx) 파싱

Excel 파일을 JSON 배열로 변환하여 처리. 열 인덱스는 0부터 시작.

```typescript
const jsonData = XLSX.utils.sheet_to_json(worksheet, {
  header: 1,     // 배열 형태로 반환
  defval: null,  // 빈 셀은 null
});
// 결과: [[2026, 1, 1, "목", ...], ...]
```

## 커밋 내역

| 해시 | 메시지 |
|------|--------|
| 86c9d9a | chor: 배포 환경설정 추가 |
| cbcd027 | chore(admin): recharts 라이브러리 추가 |
| 81c7d50 | feat(admin): 월별 평균 초과금 추이 차트 구현 |
| d23eaf2 | feat(admin): 사용자별 입력 누락 체크 API 추가 |
| 0ac8534 | feat(admin): 사용자 페이지 입력 체크 및 UX 개선 |
| b5c3d65 | fix(user): 금액 입력 필드 '원' 텍스트와 커서 겹침 수정 |
| ffe551e | chore(admin): react-hook-form 라이브러리 추가 |
| 0da74b6 | style(admin): 브랜딩 로고 및 헤더 UI 개선 |
| 09bdeb0 | fix(admin): 캘린더 근태 옵션 개선 |
| b2c5ce9 | style(admin): 대시보드 금액 표시 소수점 개선 |
| 68c12de | fix(admin): Excel import/export 열 구조 수정 |
| a1c65ce | feat(admin): import 완료 시 쿼리 무효화 및 완료 알림 추가 |
| a174933 | fix(admin): 캘린더 변경 시 사용자 현황 쿼리 무효화 |
| d8601f3 | refactor(admin): 대시보드 UI 개선 및 점심조 현황 추가 |
| 1b93176 | fix(admin): 정산 API 미정산 멤버 전체 반환 |

## 변경된 파일

- `turbo.json`
- `apps/admin/package.json`
- `apps/admin/lib/excel-parser.ts`
- `apps/admin/app/api/export/member/route.ts`
- `apps/admin/app/api/stats/trends/route.ts`
- `apps/admin/app/api/stats/incomplete-users/route.ts` (신규)
- `apps/admin/app/api/stats/settlement/route.ts`
- `apps/admin/app/(dashboard)/page.tsx`
- `apps/admin/app/(dashboard)/import/page.tsx`
- `apps/admin/app/(dashboard)/calendar/page.tsx`
- `apps/admin/app/(dashboard)/users/page.tsx`
- `apps/admin/app/(auth)/login/page.tsx`
- `apps/admin/components/Header.tsx`
- `apps/admin/components/Sidebar.tsx`
- `apps/admin/public/ACG_LOGO_GRAY.png` (신규)
- `apps/admin/public/acg_ci_white.png` (이동)
- `apps/user/components/meal-entry/steps/AmountStep.tsx`
- `pnpm-lock.yaml`

## 결과

- 15개 커밋
- +1,121 / -588 lines (18개 파일)
- Excel import 파싱 오류 해결
- 데이터 변경 시 모든 관련 페이지에서 최신 데이터 반영
- 대시보드 UI 간소화 및 점심조 현황 추가
- 사용자 입력 누락 현황 확인 기능 추가
