---
date: 2026-02-23
status: 완료
description: Admin에서 관리하는 점심조 제외 인원을 DB 기반으로 연동하고, User 앱에서 제외 대상자의 뽑기 버튼을 비활성화했습니다.
category: Frontend
type: Feature
project: Meal ACG Admin, Meal ACG User
tags:
  - React
  - TypeScript
  - Supabase
  - Next.js
  - TanStack Query
---

# 점심조 제외 인원 관리 기능 구현

> Admin에서 관리하는 점심조 제외 인원을 DB 기반으로 연동하고, User 앱에서 제외 대상자의 뽑기 버튼을 비활성화했습니다.

## 문제 / 목표

- Admin 앱에서 점심조 제외 인원을 주차별로 관리하는 기능이 구현되어 있었지만, 하드코딩된 이름 목록으로 처리되고 있었습니다.
- 이를 `lunch_group_excluded_members` DB 테이블 기반으로 전환하고, User 앱에서 제외된 유저 본인이 뽑기 버튼을 누를 수 없도록 비활성화해야 했습니다.

## 해결 과정

### 1. Supabase 타입 추가

Admin과 User 양쪽 `types.ts`에 `lunch_group_excluded_members` 테이블 타입을 추가했습니다. `member_id`, `week_start_date`, `excluded_at` 필드를 포함하며, `members` 테이블과 FK 관계를 설정했습니다.

### 2. Admin 앱 — DB 기반 제외 인원 조회/등록

기존에 `useState`로 관리하던 `excludedMemberIds`를 `useQuery`로 DB에서 조회하도록 변경했습니다. 제외 처리도 `useMutation`을 통해 API 호출 후 쿼리를 invalidate하는 방식으로 전환했습니다.

```typescript
// Before: 클라이언트 상태로 관리
const [excludedMemberIds, setExcludedMemberIds] = useState<Set<string>>(new Set());

// After: DB에서 조회 + mutation으로 등록
const { data: excludedMembersData = [] } = useQuery({
  queryKey: queryKeys.lunchGroups.excludedMembers(weekStartDate),
  queryFn: async () => { /* fetch API */ },
});

const excludedMemberIds = useMemo(
  () => new Set(excludedMembersData.map((e) => e.member_id)),
  [excludedMembersData],
);
```

### 3. User 앱 — API에서 제외 인원 반환

`/api/lunch-group` 라우트에서 `lunch_group_excluded_members` 테이블을 조회하여 `excludedMembers` 배열(이름 목록)을 응답에 포함시켰습니다. `useLunchGroup` 훅의 `LunchGroupData` 인터페이스에도 `excludedMembers?: string[]` 필드를 추가했습니다.

### 4. User 앱 — 뽑기 버튼 비활성화

`userName`과 `excludedMembers` 배열을 비교하는 `isExcluded` memo를 추가하고, 버튼의 스타일과 동작을 조건부로 변경했습니다.

```typescript
const isExcluded = useMemo(() => {
  if (!userName || !lunchGroupData?.excludedMembers) return false;
  return lunchGroupData.excludedMembers.includes(userName);
}, [userName, lunchGroupData?.excludedMembers]);
```

- 제외 대상: 버튼 disabled, 회색 배경, "점심조 배정 대상이 아닙니다" 텍스트
- 일반 유저: 기존 그라디언트 버튼 + "점심조 뽑기" 텍스트 유지

## 기술 개념 설명

### useMemo를 활용한 파생 상태

서버에서 받은 데이터(`excludedMembers`)와 클라이언트 상태(`userName`)를 조합하여 파생 상태를 계산할 때 `useMemo`를 사용합니다. 불필요한 재계산을 방지하면서도 의존성이 변경될 때만 새로운 값을 반환하므로, 배열 비교 같은 연산에 적합합니다.

### Query Invalidation 패턴

`useMutation`의 `onSuccess`에서 관련 쿼리를 invalidate하면 React Query가 자동으로 최신 데이터를 refetch합니다. 이번 구현에서는 제외 인원 등록 후 `excludedMembers` 쿼리를 invalidate하여 UI가 즉시 반영되도록 했습니다.

## 변경된 파일

- `apps/admin/lib/supabase/types.ts`
- `apps/user/lib/supabase/types.ts`
- `apps/admin/lib/query-keys.ts`
- `apps/admin/app/(dashboard)/lunch-groups/page.tsx`
- `apps/user/app/api/lunch-group/route.ts`
- `apps/user/hooks/useLunchGroup.ts`
- `apps/user/app/(content)/lunch/page.tsx`

## 결과

- Admin 앱에서 주차별 점심조 제외 인원이 DB에 저장되고, 페이지 새로고침 후에도 유지됩니다.
- User 앱에서 제외된 유저는 뽑기 버튼이 비활성화되어 "점심조 배정 대상이 아닙니다" 메시지가 표시됩니다.
- 미추첨 인원 계산 시에도 하드코딩이 제거되고 DB 기반 제외 목록이 적용됩니다.
